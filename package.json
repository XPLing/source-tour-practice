{
  "name": "3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:utils": "pnpm run --filter @xpl/utils build",
    "build:reactivity": "pnpm run --filter @xpl/reactivity build",
    "build": "pnpm run --filter='@xpl/*' build",
    "test": "vitest packages/",
    "test:once": "vitest run packages/",
    "coverage": "vitest run --coverage",
    "lint": "eslint packages/**/src/**/*.ts",
    "lint:fix": "eslint packages/**/src/**/*.ts --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-c8": "^0.29.3",
    "@vitest/coverage-istanbul": "^0.29.3",
    "typescript": "^4.9.5",
    "unbuild": "^1.1.2",
    "vitest": "^0.29.3"
  }
}
